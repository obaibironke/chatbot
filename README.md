# chatbot
This is a multi-tenant chatbot backend built on n8n. It features Google Sheets as a config base, uses the config to reply with the brand's identity, has per-client token auth, session rate limiting. It has a content moderation filter and error logging. It is powered by Llama 3.1 8B on Groq .
